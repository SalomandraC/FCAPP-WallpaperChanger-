import 'package:flutter/material.dart';

class RAnimatedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('R Анимированные')),
      body: Center(child: Text('Страница R анимированных элементов')),
    );
  }
}
