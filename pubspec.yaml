name: fcap
description: A new Flutter project.

publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.3.0
  window_size: ^0.1.0
  english_words: ^4.0.0
  provider: ^6.0.0
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/error.jpg
    - assets/back_step.png
    - assets/fons/
    - assets/fonsAnimated/
    - lib/src/plugins/FSCplugin.exe
    